<h3 align="center">Profile</h3>
<div class="row">    
      <div class="col-sm- col-sm-offset-5">
       <div class="well">

<% @user.posts.each do |post| %>
<%= post.food %>
<img src="<%= post.image%>">
<%end%>
      </div>
    </div>
 </div>   